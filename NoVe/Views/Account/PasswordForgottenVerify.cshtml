@{
    ViewData["Title"] = "Passwort Vergessen Verifizieren";
}
@if (ViewBag.Message != null)
{
    <p>@Html.Raw(ViewBag.Message)</p>
}
<div class="text-center">
    <h1 class="display-4">Login</h1>
    <div class="login-wrapper">
        <!-- Html.BeginForm(Method, Controller) -->
        @using (Html.BeginForm("PasswordForgottenCheckVerifyKey", "Account"))
        {
            <div class="login-form">
                <label class="form-login-label" name="verifyCode">Verifizierungs-Code</label>
                <input class="form-login" type="number" name="verifyCode" id="verifyCode" />

                <div class="login-buttons">
                    <input class="login-submit" type="submit" name="submit" value="Verifizierungscode Bestätigen" />
                </div>

            </div>

        }
    </div>
</div>
